wb = xlsx_package.workbook
wb.add_worksheet(name: "Eventos y Víctimas") do |sheet|
  sheet.add_row ['NUM CONSECUTIVO']
  (0..@total-1).each do |x|
    sheet.add_row [@events[x].serial_no,]
  end
end



