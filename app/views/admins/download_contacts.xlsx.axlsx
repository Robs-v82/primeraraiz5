wb = xlsx_package.workbook
wb.add_worksheet(name: "contactos_primera_raiz") do |sheet|
  @contacts.each do |contact|
    sheet.add_row [contact.category, contact.first_name, contact.name, contact.organization, contact.email, contact.mobile_phone, contact.other_phone]
  end
end